{
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "proxy": "nodemon proxy/proxy.js",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run proxy\" \"npm run client\"",
    "build": "cd proxy && npm install && cd.. && cd client && npm install && npm run build",
    "preview": "node proxy/proxy.js && cd client && npm run preview"
  },
  "dependencies": {
    "file-saver": "^2.0.5",
    "ics": "^2.41.0"
  }
}
